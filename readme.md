# OMG It's a merge conflict

## What I want

I want to merge: 

* `red` into master
* `green` into master`

The end result should have the following

* `.para1` is red
* `.para2` is green
* `h1` is blue
